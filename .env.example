# Optional: override registry target for MCP server requests
# Keep default in production; use local URL only for local development
LPM_REGISTRY_URL=https://lpm.dev

# Optional: provide token explicitly when keychain is unavailable
# Prefer `lpm login` in normal local usage
# LPM_TOKEN=lpm_your_token_here
